apiVersion: v1
kind: Service
metadata:
  name: {{ include "krisiko.fullname" . }}
  labels:
    {{- include "krisiko.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "krisiko.selectorLabels" . | nindent 4 }}
